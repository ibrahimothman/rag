The api folder represents the public contracts of the domain: 
- commands: what does the domain accepts
- events: what does the domain emits
- queries: what does the domain answers

Both commands and events shape the behaviour of the domain, queries can come later when a concrete needs appear